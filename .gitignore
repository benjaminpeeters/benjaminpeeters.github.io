_site
.bundle
.sass-cache
.jekyll-cache
.jekyll-metadata
.DS_store
.ruby-version
.tweet-cache
Gemfile.lock
assets/libs/
node_modules/
vendor
.idea
# Quarto integration - auto-generated files
# Full HTML files (not used by Jekyll, only extracted content is used)
assets/quarto/*.html
# Exception: keep content files if they exist here (though they're in _includes/)
!assets/quarto/*-content.html
# Generated content files in _includes (regenerated by bin/render-notes.sh)
_includes/quarto/*.html
# LaTeX build artifacts (anywhere in repo)
**/*.aux
**/*.log
**/*.out
**/*.fls
**/*.fdb_latexmk
**/*.synctex.gz

# But DO track PDFs in cv/ directory
!cv/*.pdf
